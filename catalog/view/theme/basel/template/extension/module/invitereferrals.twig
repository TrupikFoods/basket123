
<script type="text/javascript">
    var ir = ir || function(){(window.ir.q = window.ir.q || []).push(arguments)};
    var invite_referrals = window.invite_referrals || {}; (function() {
    invite_referrals.auth = { bid_e :'{{ encryptedKey }}',
                            bid : '{{ brandID }}', 
                            sso_key : '{{ sso_key }}',
                            t : '420', 
                            rt: '{{rt}}',
                            email : '{{ email }}',
                            mobile: '{{ mobile }}', userParams : {'fname': '{{ name }}'}};    
                            invite_referrals.async = false;
    var script = document.createElement('script');
    script.src = "//cdn.invitereferrals.com/js/invite-referrals-1.0.js";
    var entry = document.getElementsByTagName('script')[0];entry.parentNode.insertBefore(script, entry); })();

    if('{{current_page}}' == 'checkout/success'){
        ir('track',{ orderID: '{{ order_id }}', event: 'sale', email: '{{ email }}', fname: '{{ name }}', mobile:'{{ mobile }}', purchaseValue: '{{ total }}'});
    }
</script>